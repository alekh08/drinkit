const Logo = ({ className = "h-12" }) => {
    return (
        <img
            src="/drinkit-logo.png"
            alt="DRINKIT"
            className={className}
        />
    );
};

export default Logo;
